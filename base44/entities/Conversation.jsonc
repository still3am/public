{
  "name": "Conversation",
  "type": "object",
  "properties": {
    "participant_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "The two user ids in this DM"
    },
    "participant_names": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Display names indexed to match participant_ids for quick rendering"
    },
    "participant_avatars": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Avatar URLs indexed to match participant_ids"
    },
    "last_message_text": {
      "type": "string",
      "default": ""
    },
    "last_message_at": {
      "type": "string",
      "default": "",
      "description": "ISO timestamp of the most recent message, for sorting the list"
    },
    "last_sender_id": {
      "type": "string",
      "default": ""
    },
    "is_pinned": {
      "type": "boolean",
      "default": false,
      "description": "Pinned conversations sort to the top of the list"
    },
    "is_muted": {
      "type": "boolean",
      "default": false,
      "description": "Muted conversations suppress notification indicators"
    },
    "typing_user_id": {
      "type": "string",
      "default": "",
      "description": "User id of whoever is currently typing in this conversation"
    },
    "typing_at": {
      "type": "string",
      "default": "",
      "description": "ISO timestamp of the last typing indicator update"
    }
  },
  "required": [
    "participant_ids"
  ],
  "rls": {
    "create": {
      "data.participant_ids": {
        "$in": [
          "{{user.id}}"
        ]
      }
    },
    "read": {
      "data.participant_ids": "{{user.id}}"
    },
    "update": {
      "data.participant_ids": {
        "$in": [
          "{{user.id}}"
        ]
      }
    },
    "delete": {
      "data.participant_ids": {
        "$in": [
          "{{user.id}}"
        ]
      }
    }
  }
}